'use strict';

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.controlApi = undefined;

var _express = require('express');

var _express2 = _interopRequireDefault(_express);

var _httpStatusCodes = require('http-status-codes');

var _config = require('../config');

var _config2 = _interopRequireDefault(_config);

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

const router = _express2.default.Router({});
const { isLogEnabled } = _config2.default;

const controlApi = exports.controlApi = sourcesParser => {
  router.post('/set', (req, res) => {
    if (isLogEnabled) {
      console.log('API set new values'.green);
    }

    req.body.forEach(record => {
      // console.log('new record is >');
      // console.log(JSON.stringify(record, null, "  "));
      // console.log('< new record is');
      sourcesParser.switchRecord(record);
    });

    res.status(_httpStatusCodes.ACCEPTED).end();
  });

  router.post('/reset', (req, res) => {
    if (isLogEnabled) {
      console.log('API Reset'.green);
    }

    sourcesParser.resetMap();

    res.status(_httpStatusCodes.ACCEPTED).end();
  });

  return router;
};
//# sourceMappingURL=control.js.map